# 节日公益活动测试版审查

分支：`holiday-public-event-test`

版本：`1.1.2-SNAPSHOT`

## 本次测试版范围

- 在主菜单中保留每日、每周彩票入口，并在中间加入“节日公益活动”入口。
- 节日公益活动默认关闭，配置项为 `holiday.enabled: false`。
- 节日玩法使用独立数据表 `holiday_bets`，不复用每日/每周的 `tickets` 表。
- 节日玩法采用玩家之间的分池竞争：
  - 玩家选择红石、黑曜石、金块之一参与。
  - 玩家选择配置内的投注额度。
  - 开奖抽出一个结果。
  - 命中结果的玩家按自己在该分池里的投注占比分配奖励池。
  - 如果无人命中，奖励池滚入下一期。
  - 服务器只保留 `house-pool-percent` 对应的公益手续费，不凭空增发货币。

## 生产安全检查

- 未修改每日彩票和每周彩票的购票表结构。
- 未删除 `tickets`、`awards`、`ledger`、`periods`、`emails` 旧表。
- 新增数据库对象只使用 `CREATE TABLE IF NOT EXISTS` 和 `CREATE INDEX IF NOT EXISTS`。
- 旧配置升级只追加缺失的 `config-version`、`holiday`、`holiday-menu`，不重写原配置文件。
- 如果生产环境已有旧版 `holiday` 配置但缺少新字段，代码会使用兜底默认值启动。
- `holiday.enabled: false` 时不会自动提醒或自动开奖，避免活动关闭时打扰服务器。
- 管理员仍可手动开奖，便于处理已有活动投注。

## 金融流程检查

- 玩家参与节日活动时，先从玩家账户转入系统账户，再写入 `holiday_bets` 和 `HOLIDAY_BET` 流水。
- 如果扣款后数据库写入失败，会立刻尝试从系统账户退回玩家。
- 节日开奖的结果会先写 `HOLIDAY_OUTCOME`，重试时复用同一个开奖结果，避免崩服后重新随机。
- 节日手续费写为 `HOLIDAY_FEE`。
- 节日派奖写为 `HOLIDAY_PAYOUT`。
- 节日派奖失败写为 `HOLIDAY_PAYOUT_FAILED`，并且不会推进到下一期。
- 节日退款失败写为 `REFUND_FAILED`，并且不会推进到下一期。
- 无人命中时写 `HOLIDAY_ROLLOVER`，奖励池滚入下一期。

## 业务流程检查

- 玩家打开 `/lottery` 后可看到每日、节日、每周三个入口。
- 节日活动关闭时，节日二级菜单显示活动未开放，玩家不能下注。
- 节日活动开启后，玩家进入二级菜单选择红石、黑曜石、金块和投注额度。
- 菜单展示当前奖池、该分池金额、该分池玩家数、该分池注数、个人投注次数。
- 菜单不再展示估算回报倍率，避免玩家误解为固定赔率或保底收益。
- 节日活动关闭时，菜单入口和管理员状态查询均不再展示开奖倒计时。
- 玩家选择一个分池后，其他分池锁定，但仍展示对应分池统计。
- 管理员退款成功后会通过 SMTP 向玩家发送退款通知。
- 节日公益活动中奖后会通过 SMTP 向玩家发送节日活动中奖通知。
- 管理员可用 `/lottery draw holiday` 手动开奖。
- 管理员可用 `/lottery preview holiday` 查看当前期状态。
- 管理员可用 `/lottery period holiday` 查看当前期。
- 管理员可用 `/lottery history holiday` 查看上一期节日活动结果。
- 管理员可用 `/lottery pool add holiday <金额>` 给当前节日活动奖池加注。

## 边界检查

- 活动关闭：不能下注，不自动开奖。
- 没有投注：不开奖，进入下一期，保留已有滚入奖池。
- 奖池不足：退款本期投注；退款失败则不推进期数。
- 无人命中：不退款，奖励池滚入下一期。
- 多人命中：按同分池投注金额占比分配。
- 同一玩家多次命中同一结果：按该玩家在命中分池的总投注金额合并派奖。
- 派奖中断后重启：已支付的玩家通过流水跳过，未支付玩家继续补发；全部成功后才推进期数。

## 1.1.2 上线前审计结论

本阶段审计结论：允许作为当前阶段版本推进，但以下事项需记录，后续如进入更严格金融审计或扩大生产规模，应继续加固。

### 汉化结论

- 玩家菜单、主要系统消息、邮件模板、广播、提醒、管理员帮助均已使用中文表达。
- 命令本体和参数 token 保持英文，例如 `open`、`reload`、`daily`、`weekly`、`holiday`、`redstone`、`obsidian`、`gold`。这是 Bukkit 插件命令的常规设计，便于控制台、TRMenu、脚本、权限系统和文档统一，不视为汉化缺陷。
- 后台日志、异常信息、部分开发者注释仍为英文。这些内容主要面向维护者，不影响玩家侧体验。

### 当前接受的已知风险

- 部分资金流程仍是先调用经济插件转账，再写入最终成功流水。如果经济转账成功但数据库写入失败，理论上存在重复补发或账实不一致风险。
- 管理员退款、自动退款、派奖、节日派奖均有类似的“外部经济系统成功、SQLite 记录失败”边界情况。
- 管理员加奖池流程先给系统账户入账，再写插件 rollover 和流水。如果数据库失败，可能出现账户余额增加但插件奖池未增加。
- 购票写库失败后会尝试退款，但每日/每周路径对退款失败的审计记录不如节日路径完整。
- 以上问题当前判断为可通过人工流水、数据库记录和系统账户余额进行恢复处理，暂不在本阶段继续修改。

### 后续建议

- 后续如要做强金融一致性，应引入 `PENDING` / `PAID` / `FAILED` 状态或结算批次表。
- 所有经济出账建议改成“先落库待支付记录，再执行经济支付，最后标记完成”的补偿式流程。
- 管理员加奖池建议改成可回滚或可补偿流程。
- 对自动派奖、退款、管理员退款统一抽象为同一套支付执行器，减少三种彩票类型之间的流程差异。

## 构建结果

已通过：

```powershell
.\.tools\apache-maven-3.9.9\bin\mvn.cmd "-Dmaven.repo.local=.m2repo" -DskipTests package
```

可测试 jar：

```text
target/lottery-1.1.2-SNAPSHOT.jar
```

不要部署：

```text
target/original-lottery-1.1.2-SNAPSHOT.jar
```

## 暂不建议直接合并主分支的原因

该版本已经完成测试版开发、本地构建和上线前审计记录。合并主分支前仍建议先在测试服验证：

- XConomy 对系统账户收付款是否稳定。
- SQLite 数据库升级后旧数据是否完全保留。
- 节日二级菜单在真实 Bukkit 1.20.1 混合端里显示是否符合预期。
- 开奖广播、历史查询、流水查询是否符合服务器运营习惯。
